# CoolRoute Tokyo 开发规则

本文件中的规则适用于整个仓库。后续所有贡献者和 Codex 会话都必须遵守。

## 项目范围

- CoolRoute Tokyo 是一款面向东京高温环境的步行路线推荐 Web 应用。
- Demo 只比较三种路线意图：**Fastest Route**、**Balanced Route** 和 **Coolest Route**。
- 路线比较基于道路网各边的模型估计热暴露。
- 第一版 Demo 必须限定在东京约 2–3 km 的小范围研究区域内。
- 优先确保黑客松 Demo 稳定，不要过早设计大型生产系统。

## 必须遵守的架构

- 最终正式应用必须可完整部署到 **GitHub Pages**。
- 正式运行时必须是完全静态的，不得依赖任何线上后端服务器。
- 前端技术栈为 **React + Vite + JavaScript**。
- 地图渲染使用 **MapLibre GL JS**。
- 最终路径规划逻辑必须在浏览器端 JavaScript 中执行。
- 不得引入 FastAPI、Flask、Node Server、数据库、PostGIS、Docker 或其他线上服务器运行环境。
- Python 只允许用于开发阶段或构建阶段的离线 GIS 预处理。
- 允许使用的 Python GIS 库为 GeoPandas、Shapely、OSMnx、NetworkX、Pandas 和 PyArrow。
- 运行时需要的文件必须生成为适合放入 `public/data/` 并由 GitHub Pages 托管的静态资源。

## 数据真实性

- 禁止伪造、模拟或错误标注任何东京官方开放数据。
- 每个真实数据集都必须记录来源名称、来源 URL 或标识符、已知许可证、获取日期、空间覆盖、时间覆盖和预处理说明。
- 如果真实数据源无法获取，必须创建或记录数据适配器，并暴露明确的数据缺失状态。不得悄悄用虚构官方数据替代。
- 任何官方数据获取失败时，禁止自动生成假数据、把合成数据说成真实东京数据、悄悄切换到 Kaggle 或第三方博客、使用无法验证来源的数据，或为了让程序运行而硬编码“看起来合理”的值。
- 官方数据获取失败后，必须明确报告失败、保留 Loader，并要求人工提供官方文件；不得更换来源以掩盖问题。
- 合成 fixture 只能用于单元测试或开发测试，必须明确标记为 `synthetic`，且不得进入正式 Demo Data。
- 派生值必须能与原始观测值区分。
- 缺失特征必须使用已记录的回退或排除规则，并保留机器可读的数据质量标记。
- `data/raw/` 中的原始文件永远不得就地修改。

## 术语与声明

- 必须统一使用以下术语：**Heat Exposure Score**、**Heat Exposure Index** 和 **热暴露评分**。
- 该指标是用于比较路线的模型估计，不是临床或流行病学结果。
- 禁止将指标描述为 **中暑概率**、医疗风险或经医学验证的风险降低比例。
- 禁止声称某条路线能预防疾病、保证安全或带来经临床证明的收益。
- 面向用户的文案必须说明相关数据缺口，并指出实际环境可能与模型估计不同。

## 功能边界

- 不得加入 LLM、登录、用户账户、数据库、聊天机器人或无关功能。
- M1 不得实现 Dijkstra、Fastest Route、Balanced Route、Coolest Route、Heat Exposure Score 计算、GIS Spatial Join、PLATEAU 阴影或天气模型。
- 离线数据准备、静态数据契约、浏览器端寻路、UI 组件和通用工具必须按目录和职责分离。
- 优先使用小型、明确的模块和稳定数据契约，避免过早抽象。
- 扩大地理范围、运行时基础设施或数据声明前，必须先明确更新项目规格。

## 质量门槛

- 架构和依赖审查时必须始终检查静态部署约束。
- 连接真实处理数据前，必须使用小型、确定性 fixture 测试浏览器端寻路逻辑。
- 发布处理后的地理空间文件前，必须验证 schema、坐标参考系、边界、缺失值和来源记录。
- 声明发布就绪前，必须验证 GitHub Pages 资源路径和正式构建。
- 文档必须明确标注数据源处于已接入、不可用或计划中的哪一种状态；“计划中”不等于“可用”。
